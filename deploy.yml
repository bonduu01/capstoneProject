---
- name: Transfer and execute a script.
  hosts: all
  remote_user: root
  tasks:
     - name: Transfer the script
       copy: src=/var/lib/jenkins/workspace/capstoneroject_master/deploy.sh dest=/app/ mode=0777

     - name: Execute the script
       command: sh /app/deploy.sh